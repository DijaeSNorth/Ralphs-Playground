# Gym Buddy Catcher

A low-poly browser game prototype about catching friendly gym buddies around an outdoor fitness park.

## Play

- Move with `WASD`, arrow keys, a touch D-pad, or the left stick on a standard gamepad.
- Press `Space`, `E`, the touch Catch button, or gamepad `A` to throw a Buddy Ball.
- Hold `Shift` or gamepad right trigger to sprint while stamina lasts.

The game is designed as a static Vite app and can be deployed to GitHub Pages with the included workflow.

## Local Development

```bash
npm install
npm run dev
```

## Production Build

```bash
npm run build
```

The generated visual concept is saved at `public/concepts/gym-buddy-concept.png`.
